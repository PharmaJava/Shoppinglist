// X/Twitter no hereda la imagen de Open Graph cuando se usa el convenio de
// fichero, así que se sirve la misma imagen desde su propia ruta.
export { alt, contentType, default, size } from "./opengraph-image";
